{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
<div class="container">
    <a class="btn btn-primary btn-4 btn-4c icon-arrow-right mb-4 mt-5" href="{{ path('article_index') }}">Retourner aux articles</a>

    <div class="div mb-3">
        {{ include('article/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="btn-delete">
        {{ include('article/_delete_form.html.twig') }}
    </div>
</div>

{% endblock %}
