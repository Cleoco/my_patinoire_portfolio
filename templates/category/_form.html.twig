<div class="mb-2">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary btn-4 btn-4c icon-arrow-right mt-5">{{ button_label|default('Enregistrer') }}</button>
    {{ form_end(form) }}
</div>
