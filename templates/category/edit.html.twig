{% extends 'base.html.twig' %}

{% block title %}Modifier une prestation{% endblock %}

{% block body %}
<div class="container">
    <a class="btn btn-primary btn-4 btn-4c icon-arrow-right mb-4 mt-5" href="{{ path('category_index') }}">retourner aux prestations</a>
      
    <div class="div mb-3">
      {{ include('category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="btn-delete">
        {{ include('category/_delete_form.html.twig') }}
    </div>
</div>

{% endblock %}


